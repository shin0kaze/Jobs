(() => {
    console.log('loaded');
    let appContainer = document.getElementById('main-container');

    function initApp(title) {

    }
    function addPeople() {}
    function getPeoples() {}

    function changeDropdownCaption(item) {
        
    }
    initApp('test');
})();
/*
        let appTitle = document.createElement('h2')
        appTitle.innerHTML = title
        appContainer.appendChild(appTitle)
        filterByPost = document.getElementById('filter-by-post')
        filterByPost.onclick = () => {}; */